# .ebextensions/cfn-signal-retry.config
container_commands:
  01_retry_signal:
    command: |
      for i in {1..5}; do
        cfn-signal -e 0 --stack ${AWS::StackName} --resource AWSEBAutoScalingGroup --region ${AWS::Region} && break || sleep 15
      done
